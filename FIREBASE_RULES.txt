rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read access cho tất cả collections
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Chỉ đọc, không cho ghi từ client
    }
  }
}

